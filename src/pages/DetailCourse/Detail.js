// ** Reactstrap Imports
import { Card, CardHeader, CardTitle, CardBody } from "reactstrap";

const Detail = () => {
  return (
    <Card className="min-h-70">
      <CardHeader>
        <CardTitle tag="h4">جزئیات دوره</CardTitle>
      </CardHeader>
      <CardBody className="pt-1"></CardBody>
    </Card>
  );
};

export default Detail;
